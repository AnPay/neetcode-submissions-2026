For each pattern, say 1-liner + use case:

Strategy → “Replace if-else with interchangeable behavior” , payment

Factory → “Encapsulate object creation”, shape/vehicle

Singleton → “Ensure one instance globally” thread safe version

Observer → “Notify multiple dependents on state change”  stock system
