For each pattern, say 1-liner + use case:

Strategy → “Replace if-else with interchangeable behavior”

Factory → “Encapsulate object creation”

Singleton → “Ensure one instance globally”

Observer → “Notify multiple dependents on state change”
